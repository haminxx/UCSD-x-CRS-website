# Firebase Functions (optional / unused for free tier)

Recruitment chat now runs on **Render** free tier — see `chat-server/README.md`.

Firebase Cloud Functions require the **Blaze** plan, so this `functions/` folder
is kept only as a backup if you later upgrade Firebase.
