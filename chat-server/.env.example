# Copy to .env for local runs — never commit real keys
GEMINI_API_KEY=
# Free-tier default; server also falls back to gemini-2.5-flash-lite / gemini-flash-latest
GEMINI_MODEL=gemini-2.5-flash
PORT=10000
